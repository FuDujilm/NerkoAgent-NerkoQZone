"""兼容层：提供老插件期望的 `src` 包接口（最小实现）。

这个包会被放在插件目录下，优先被 Python 导入，从而避免主程序找不到全局 `src` 包的问题。
实现为轻量的 shim：在可用时会复用 `nekro_agent` 的相应模块，否则提供安全的默认实现。
"""

__all__ = [
    'common',
    'plugin_system',
    'chat',
]
